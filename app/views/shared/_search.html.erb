<%= form_tag locations_path, method: :get do %>
  <%= search_field_tag :query,
    @locations.any? ? params[:query] : alert,
    class: 'search-bar',
    id: "location_address",
    placeholder: "Search"
  %>
<% end %>
